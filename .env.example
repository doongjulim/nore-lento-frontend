# nore-lento-backend 의 베이스 URL (마지막 슬래시 없이)
# Spring Boot 기본 포트가 8080. 백엔드 SecurityConfig 의 CORS 가
# http://localhost:5173 (Vite dev) 만 허용하므로, 프론트도 5173 으로 띄울 것.
VITE_API_BASE_URL=http://127.0.0.1:8080
